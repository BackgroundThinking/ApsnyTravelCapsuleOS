> # Security Hardening Guide

**Version:** 1.0
**Date:** December 2, 2025

---

## 1. Overview

This document outlines the security measures implemented in ApsnyTravelCapsuleOS and provides recommendations for maintaining a secure deployment.

---

## 2. Content Security Policy (CSP)

A Content Security Policy (CSP) should be configured to prevent XSS attacks and other injection vulnerabilities.

### 2.1. Recommended CSP Headers

Add the following headers to your web server configuration:

```
Content-Security-Policy: default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data: https:; font-src 'self'; connect-src 'self' https:; frame-ancestors 'none'; base-uri 'self'; form-action 'self';
```

### 2.2. Nginx Configuration Example

```nginx
add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data: https:; font-src 'self'; connect-src 'self' https:; frame-ancestors 'none'; base-uri 'self'; form-action 'self';" always;
```

---

## 3. HTTPS & TLS

Always use HTTPS in production to encrypt data in transit.

### 3.1. SSL/TLS Configuration

- Use TLS 1.2 or higher
- Use strong cipher suites
- Implement HSTS (HTTP Strict Transport Security)

### 3.2. Recommended Nginx Configuration

```nginx
ssl_protocols TLSv1.2 TLSv1.3;
ssl_ciphers HIGH:!aNULL:!MD5;
ssl_prefer_server_ciphers on;
add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
```

---

## 4. HTTP Security Headers

Implement the following HTTP security headers:

| Header                   | Value                                      | Purpose                       |
| :----------------------- | :----------------------------------------- | :---------------------------- |
| `X-Content-Type-Options` | `nosniff`                                  | Prevents MIME type sniffing   |
| `X-Frame-Options`        | `DENY`                                     | Prevents clickjacking         |
| `X-XSS-Protection`       | `1; mode=block`                            | Enables XSS protection        |
| `Referrer-Policy`        | `strict-origin-when-cross-origin`          | Controls referrer information |
| `Permissions-Policy`     | `geolocation=(), microphone=(), camera=()` | Restricts browser features    |

### 4.1. Nginx Configuration Example

```nginx
add_header X-Content-Type-Options "nosniff" always;
add_header X-Frame-Options "DENY" always;
add_header X-XSS-Protection "1; mode=block" always;
add_header Referrer-Policy "strict-origin-when-cross-origin" always;
add_header Permissions-Policy "geolocation=(), microphone=(), camera=()" always;
```

---

## 5. CORS Configuration

Configure Cross-Origin Resource Sharing (CORS) to allow requests only from trusted origins.

### 5.1. Nginx Configuration Example

```nginx
add_header Access-Control-Allow-Origin "https://apsnytravel.com" always;
add_header Access-Control-Allow-Methods "GET, HEAD, OPTIONS" always;
add_header Access-Control-Allow-Headers "Content-Type" always;
```

---

## 6. Dependency Security

Regularly audit dependencies for known vulnerabilities.

### 6.1. Checking for Vulnerabilities

```bash
pnpm audit
npm audit
```

### 6.2. Updating Dependencies

```bash
pnpm update
npm update
```

---

## 7. Environment Variables

Never commit sensitive information to version control.

### 7.1. `.env` File Management

- Create a `.env.example` file with placeholder values
- Add `.env` and `.env.*.local` to `.gitignore`
- Document all required environment variables

---

## 8. Data Protection

### 8.1. User Data

- Minimize collection of personal data
- Implement privacy policy
- Comply with GDPR and other regulations

### 8.2. Analytics

- Use privacy-respecting analytics (e.g., Umami, Plausible)
- Avoid third-party tracking scripts when possible
- Implement cookie consent if necessary

---

## 9. Monitoring & Logging

### 9.1. Security Monitoring

- Monitor access logs for suspicious activity
- Set up alerts for unusual traffic patterns
- Regularly review error logs

### 9.2. Incident Response

- Have a documented incident response plan
- Know how to quickly disable or rollback deployments
- Keep backups of critical data

---

## 10. Regular Security Audits

- Conduct security audits at least quarterly
- Use tools like OWASP ZAP or Burp Suite for penetration testing
- Review security headers with tools like Security Headers (securityheaders.com)

---

**Generated by:** Manus AI
